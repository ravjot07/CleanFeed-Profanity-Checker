import React from 'react';
import ReactDOM from 'react-dom';

const Options: React.FC = () => {
  return <h1>CleanFeed Options</h1>;
};

ReactDOM.render(<Options />, document.getElementById('root'));
